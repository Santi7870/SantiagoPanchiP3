<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SantiagoPanchiP3.ViewModels"
             x:Class="SantiagoPanchiP3.Views.SearchPage">
   <ContentPage.BindingContext>
    <vm:SearchViewModel />
</ContentPage.BindingContext>

<StackLayout>
    <Entry x:Name="spanchi_entrySearch" Placeholder="Buscar película..." />
    <Button x:Name="spanchi_btnSearch" Text="Buscar" Command="{Binding SearchCommand}" />
    <Button x:Name="spanchi_btnClear" Text="Limpiar" Clicked="OnClearClicked" />
    <Label x:Name="spanchi_lblResult" Text="" />
</StackLayout>
</ContentPage>
